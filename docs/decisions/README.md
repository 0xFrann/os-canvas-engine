# Architecture decisions

ADRs live here as `NNN-<slug>.md`. Each one follows: **Context** → **Options** (alternatives considered) → **Decision** → **Why** → **Consequences**, plus a closing **References** section.
